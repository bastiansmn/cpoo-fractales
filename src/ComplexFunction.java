import java.util.function.Function;

public interface ComplexFunction extends Function<Complex, Complex> {
    // TODO Créer aussi un parser d'expression depuis un string, vers une ComplexFunction (String vers un arbre synthaxique, puis arbre vers fonction)
}
